name: Test

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: python3 -m pip install pyyaml

      - name: Run test suite
        run: ./test.sh exec --cases all